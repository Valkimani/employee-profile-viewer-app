import React from 'react';
import "../styles/SearchBtn.css";

const SearchBtn = () => {
    return (
        <div>
            <button>Search</button>
        </div>
    );
};

export default SearchBtn;